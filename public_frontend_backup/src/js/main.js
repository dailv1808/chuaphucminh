// Import CSS toàn cục
import '../css/style.css';       // CSS chung (Tailwind)

// Import AlpineJS
import Alpine from 'alpinejs';
window.Alpine = Alpine;
Alpine.start();

// Import các file JS khác (nếu cần)
